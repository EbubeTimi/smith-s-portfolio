const {chromium}=require(process.env.PORTFOLIO_NODE_MODULES+'/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});for(const width of [1440,390]){const page=await browser.newPage({viewport:{width,height:900},reducedMotion:'reduce'});await page.goto('http://127.0.0.1:4174/preview.html#films');for(const name of ['trailer','timi','emma']){const button=page.locator('[data-play="'+name+'"]');await button.focus();await page.keyboard.press('Tab');await page.keyboard.press('Shift+Tab');const focus=await button.evaluate(el=>({active:el===document.activeElement,offset:getComputedStyle(el).outlineOffset,width:getComputedStyle(el).outlineWidth}));assert.deepEqual(focus,{active:true,offset:'-6px',width:'3px'});await button.scrollIntoViewIfNeeded();await page.screenshot({path:'evidence/focus-'+name+'-'+width+'.png'});}await page.close();}await browser.close();console.log('PASS visible inset keyboard focus on all three film controls at 1440 and390');})().catch(e=>{console.error(e);process.exit(1)});
